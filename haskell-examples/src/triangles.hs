-- 直角三角形
-- 各辺の長さはすべて整数
-- 各辺の長さは10以下
-- 各辺の長さの合計は24に等しい

-- a^2 + b^2 == c^2
-- a < c, b < a 
-- a + b + c == 24

rightTriangles = [(a,b,c) | c <- [1..10], a <- [1..c], b <- [1..a], a^2 + b^2 == c^2, a + b + c == 24]
